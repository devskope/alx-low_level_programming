#include "main.h"

/**
 * _abs - Compute the absolute value of an integer
 * @n: integer to process
 *
 * Description: print the absolute value of the number n.
 *
 * Return: Absolute value of n
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	return (n * -1);
}
